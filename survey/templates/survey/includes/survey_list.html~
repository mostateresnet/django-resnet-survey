<li class="list_reset">
  <span id="{{ catid }}" class="accordion_sidebar accordion">{{ title }}</span>
  <ul>
    {% for survey_item in surveys %}
      <li>
        <a {% if survey == survey_item %}class='current_survey'{% endif %} href="{% url surveydashboard survey.slug %}">{{ survey_item }}</a>
      </li>
    {% endfor %}
  </ul>
</li>